# AnonChat 💬

**AnonChat** — система чата с микросервисной архитектурой, написанная на **Golang** и **React**. **(frontend написан с помощью cursor)**

Она обеспечивает **регистрацию и авторизацию пользователей**, **создание и управление комнатами чата**, а также **обмен сообщениями в реальном времени** через WebSocket.

Использует **микросервисную архитектуру** с API Gateway, **PostgreSQL** для хранения данных и **React** для веб-интерфейса.

---

## 🧩 Основные возможности

| Функция              | Описание                                              |
| -------------------- | ----------------------------------------------------- |
| **Регистрация пользователя** | Создание аккаунта с логином, email и паролем         |
| **Авторизация пользователя** | Вход и получение JWT токенов (access/refresh)       |
| **Управление токенами** | Автоматическое обновление и валидация токенов        |
| **Создание комнаты** | Создание комнат чата с именем и лимитом пользователей |
| **Присоединение к комнате** | Присоединение к существующим комнатам по ID          |
| **Обмен сообщениями в реальном времени** | Мгновенный обмен сообщениями через WebSocket       |
| **Управление комнатами** | Администратор может удалять созданные комнаты        |
| **Ограничение запросов** | Защита от спама и злоупотреблений                    |
| **Структурированное логирование** | Логирование запросов через собственный логгер      |
| **Автообновление токенов** | Автоматическое обновление токенов на фронтенде       |

---

## ⚙️ Технологии

### Бэкенд

| Слой              | Технологии                                  |
| ----------------- | ------------------------------------------- |
| **Язык**          | Go 1.25.4                                   |
| **Фреймворк**     | Gin                                         |
| **WebSocket**     | Gorilla WebSocket                           |
| **База данных**   | PostgreSQL 17                               |
| **Аутентификация** | JWT (access / refresh tokens)               |
| **Промежуточное ПО** | RateLimiter, Logger, Auth                  |
| **Конфигурация**  | YAML файлы                                  |
| **Архитектура**   | Микросервисы (Gateway + Auth Service)       |

---

## 🚀 Эндпойнты API

### Авторизация

| Метод | Путь                     | Описание                                  |
| ----- | ------------------------ | ----------------------------------------- |
| `GET`  | `/api/v1/status`         | Проверка статуса сервиса                  |
| `POST` | `/api/v1/auth/register`  | Регистрация нового пользователя           |
| `POST` | `/api/v1/auth/login`     | Авторизация и получение токенов           |
| `POST` | `/api/v1/auth/refresh`   | Обновление access токена                  |
| `POST` | `/api/v1/auth/logout`    | Выход и завершение сессии                 |
| `GET`  | `/api/v1/auth/validate`  | Валидация access токена                   |

### Комнаты

| Метод   | Путь                     | Описание                                  |
| ------- | ------------------------ | ----------------------------------------- |
| `GET`    | `/api/v1/rooms/ws`       | WebSocket подключение для чата            |
| `POST`   | `/api/v1/rooms/create`   | Создание новой комнаты чата               |
| `DELETE` | `/api/v1/rooms/remove`   | Удаление комнаты (только админ)           |

---

## 🔒 Процесс авторизации

1. **Регистрация** — пользователь отправляет `login`, `email`, `password`.
   → получает `access_token` (15 мин) и `refresh_token` (7 дней).

2. **Вход** — вводит `email` и `password`.
   → получает пару токенов.

3. **API запросы** — все запросы (кроме register/login) требуют `Authorization: Bearer <access_token>`.

4. **Обновление токена** — при истечении access токена:
   - Frontend автоматически обновляет токен через `/auth/refresh`
   - Получает новую пару токенов
   - Повторяет оригинальный запрос

5. **WebSocket** — подключение требует `room` ID и `token` (access token) в query параметрах.

6. **Выход** — удаляет refresh токен из БД, завершая сессию.

---

## 💬 Процесс чата

1. **Создание комнаты** — авторизованный пользователь создает комнату с именем и лимитом пользователей.
   → получает уникальный `room_id`.

2. **Присоединение к комнате** — пользователь может:
   - Присоединиться к созданной комнате из списка
   - Ввести ID комнаты для подключения

3. **WebSocket соединение** — устанавливается соединение:
   ```
   ws://host:port/api/v1/rooms/ws?room=<room_id>&token=<access_token>
   ```

4. **Обмен сообщениями** — сообщения отправляются через WebSocket и транслируются всем участникам комнаты в реальном времени.

5. **Управление комнатой** — администратор комнаты может удалить её, что закроет все активные соединения.

---

## 📄 Лицензия

**MIT License** — свободное использование и модификация проекта.

---

## 🤝 Вклад в проект

Приветствуются pull requests и issues! Пожалуйста, убедитесь, что код соответствует стилю проекта.

---

## 📧 Поддержка

Для вопросов и предложений создавайте issues в репозитории проекта.
